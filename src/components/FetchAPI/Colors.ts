export type Colors = {
    imageBg: string;
    smoothText: string;
    categoryBg: string;
}

export default ({
    imageBg: '#8252b969',
    smoothText: '#fff',
    categoryBg: '#00000059'
} as Colors)